spring:
  config:
    import: optional:file:.env[.properties]

bot:
  name: ${BOT_NAME}
  token: ${BOT_TOKEN}
  request_delay: ${MESSAGE_RECORDING_DELAY}
  command:
    prefix: ${COMMAND_PREFIX}
    get_report: ${COMMAND_GET_REPORT}


proxy:
  post:
    url: ${POST_URL}